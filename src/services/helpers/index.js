// export all helper functions
import formatEndpointResponseHelper from './formatEndpointResponse';
import celsiusToFahrenheitHelper from './celsiusToFahrenheit';
import fahrenheitToCelsiusHelper from './fahrenheitToCelsius';

export const formatEndpointResponse = formatEndpointResponseHelper;
export const celsiusToFahrenheit = celsiusToFahrenheitHelper;
export const fahrenheitToCelsius = fahrenheitToCelsiusHelper;
